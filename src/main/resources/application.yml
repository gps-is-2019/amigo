spring:
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 30MB
      max-request-size: 50MB
  profiles:
      active: "dev"
  main:
    banner-mode: "off"